<%# app / views / articles / index.js.erb %>
<% js = escape_javascript(
    render(partial: 'articles/shared/list', locals: { articles: @articles })
) %>
    $("#filterrific_results").html("<%= js %>");